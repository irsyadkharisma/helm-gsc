apiVersion: v1
auth:
  username: geoserver
  password: sodAgemb7r4$
  vhost: /

service:
  type: ClusterIP

resources:
  requests:
    cpu: 250m
    memory: 256Mi
  limits:
    cpu: 1
    memory: 1Gi

persistence:
  enabled: true
  size: 5Gi
  storageClass: standard

metrics:
  enabled: true

